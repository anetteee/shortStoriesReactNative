import { StyleSheet } from "react-native";

const SearchMenu = StyleSheet.create({
  header: {
    fontSize: 20,
    color: "white",
    paddingTop: 5,
    textAlign: "center",
  },
});

export default SearchMenu;
